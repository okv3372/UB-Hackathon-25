@page "/{userId}/ClassList"

<h1>Class List</h1>
<p>This is a placeholder page for listing classes.</p>

@if (!string.IsNullOrWhiteSpace(UserId))
{
	<p>Viewing classes for user: <strong>@UserId</strong></p>
}

<button class="btn btn-primary" @onclick="NavigateToClass">Go To Sample Class</button>

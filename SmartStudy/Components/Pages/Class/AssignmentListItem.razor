@* AssignmentListItem.razor *@
@namespace SmartStudy.Components.Pages

<div class="assignment-item">
    <div class="assignment-details">
        <h2 class="assignment-name">@AssignmentName</h2>
        <p class="assignment-file">
            <span class="file-icon">📄</span>
            @GradedFileName
        </p>
    </div>
    <button class="view-button" @onclick="NavigateToDetails">
        View Details <span class="arrow">→</span>
    </button>
</div>

<style>
    /* AssignmentListItem.razor CSS - Inlined */
    .assignment-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #FFFFFF; /* White */
        padding: 25px 30px;
        margin-bottom: 20px; /* Space between items */
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0, 78, 137, 0.08); /* Soft shadow with 004E89 base */
    border: 1px solid #004E89; /* Thin outline using requested deep blue */
        transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        max-width: 800px; /* Example max-width for a single item */
        width: 100%; /* Ensure it takes full width up to max-width */
        margin-left: auto; /* Center the item if it has max-width */
        margin-right: auto;
    }

    .assignment-item:hover {
        transform: translateY(-3px); /* Slight lift effect on hover */
        box-shadow: 0 6px 20px rgba(0, 78, 137, 0.12); /* Enhanced shadow on hover */
    }

    .assignment-details {
        flex-grow: 1;
        margin-right: 20px; /* Space between text and button */
    }

    .assignment-name {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Or your preferred modern font */
        color: #004E89; /* Deep Blue */
        font-size: 1.4em;
        margin-bottom: 8px;
        font-weight: 600;
    }

    .assignment-file {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Or your preferred modern font */
        color: #1A659E; /* Medium Blue */
        font-size: 0.95em;
        display: flex;
        align-items: center;
    }

    .file-icon {
        margin-right: 8px;
        color: #1A659E; /* Match file text color */
    }


    .view-button {
        background-color: #FF6B35; /* Vibrant Orange */
        color: white;
        padding: 12px 22px;
        border: none;
        border-radius: 6px;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Or your preferred modern font */
        font-size: 1em;
        cursor: pointer;
        transition: background-color 0.2s ease, transform 0.2s ease;
        display: flex;
        align-items: center;
        white-space: nowrap; /* Prevent button text from wrapping */
    }

    .view-button:hover {
        background-color: #E65A2C; /* Slightly darker orange on hover */
        transform: translateY(-1px); /* Subtle lift */
    }

    .view-button:active {
        background-color: #CC4E25; /* Even darker on click */
        transform: translateY(0);
    }

    .arrow {
        margin-left: 8px;
        font-weight: bold;
    }
</style>